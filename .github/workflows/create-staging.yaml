name: test 

on:
  push:
    tags:
      - '*release-[0-9]+.[0-9]+.[0-9]+'
      - '*release-[0-9]+.[0-9]+.*'
  workflow_dispatch:
    inputs:
      deploy_image_tag:
        description: deploy image tag
        type: string
        default: latest

jobs:
  run-repository-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Api
        run: |
          TOKEN=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
          OWNER=cw-yn-test2
          REPO=test-repo2

          curl \
            -X POST \
            -H "Authorization: token ${{secrets.MY_GITHUB_ACCESS_TOKEN}}" \
            -H "Accept: application/vnd.github.v3+json" \
            https://api.github.com/repos/$OWNER/$REPO/dispatches \
            -d '{"event_type":"test-sasaki","client_payload":{"env": "sasaki-dev"}}'  
      - name: Create GitHub dispatch event
        run: |
          curl --request POST 'https://api.github.com/repos/cw-yn-test2/test-repo2/dispatches' \
          --header 'Authorization: Bearer ${{secrets.MY_GITHUB_ACCESS_TOKEN}}' \
          --header 'Content-Type: application/json' \
          --data-raw '{
            "event_type": "test-repo1-updated",
            "client_payload": { "customField": "sasaki-test" }
          }'