name: Example workflow for Node using Snyk
on: push
jobs:
  snyk-install:
  steps:
    - uses: actions/checkout@v2
      run: |
          cd work
          snyk test --json | snyk monitor
      env: SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}